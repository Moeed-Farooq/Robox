/**
 * Balanced in-app point rewards.
 * These are virtual points only — not real Robux, cash, or gift cards.
 */
export const POINT_REWARDS = {
  JAIL_BREAK_ESCAPE: 25,
  BLOX_FRUITS_WIN: 20,
  WORD_QUIZ_COMPLETE: 15,
  BLOCK_PUZZLE_GAME_OVER: 10,
  DAILY_QUIZ_COMPLETE: 15,
};

export const REWARD_LABEL = 'in-app points';
